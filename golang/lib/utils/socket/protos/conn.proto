syntax = "proto3";
package socketprotos.conn;

option go_package = "socket";

message ConnParams {
    int64 PingInterval = 1;
    int64 PingTimeout = 2;
    string SID = 3;
    repeated string Upgrades = 4; 
}